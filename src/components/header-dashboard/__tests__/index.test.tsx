import { renderAllProviders } from "@/src/utils/renderAllProviders";
import HeaderDashboard from "..";

describe("<HeaderDashboard />", () => {
  it("should render correctly", () => {
    // Arrange
    renderAllProviders(<HeaderDashboard />);
  });
});
